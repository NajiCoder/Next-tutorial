"use client";

// we need to import the bootstrap classes that we want to use in the server side component here so that they are available in the client side component

export {
  Button,
  Container,
  SSRProvider,
  Alert,
  Spinner,
} from "react-bootstrap";
